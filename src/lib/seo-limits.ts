/** Límites para la sección "Enriquecimiento y SEO" (nueva carga). */
export const SEO_LIMITS = {
  resumen: 300,
  maxKeywords: 8,
  keywordMaxLength: 30,
} as const

/** Límites para etiquetas de taxonomía (sección Categorías). */
export const ETIQUETAS_LIMITS = {
  maxEtiquetas: 8,
  etiquetaMaxLength: 40,
} as const
